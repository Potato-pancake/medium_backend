spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/medium
    username: root
    password: 1234

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    open-in-view: false

  jwt:
    secret_key: ${secret_key}
    prefix: ${prefix}

server:
  port: 8090